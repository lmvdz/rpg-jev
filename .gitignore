# dependencies and build output
node_modules/
dist/
coverage/
*.tsbuildinfo

# secrets: never commit keys
.env
.env.*
!.env.example

# logs and OS noise
*.log
.DS_Store
Thumbs.db

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# saved nights (the event log is the save)
saves/
# the friction report is generated; nights handed in and the loop switch are tracked
playtests/friction.md
.claude/worktrees/
# the development loop's working files: prompts sent, replies, attempts, journal, lock
.sdlc/

# Reproducible thermal enumeration output; summary findings are tracked separately.
validation/thermal-learning/runs/
