# Historical poc assertions

These are the original 15 assertions and fixtures from the attached `poc`
working-tree experiment. They were run against uncommitted matter files on top
of `47d59de`, not against the newer sandbox implementation.

They are preserved as evidence, outside the test-discovery glob. Their relative
imports assume installation under `packages/core/test/matter/` in that historical
working tree; the uncommitted production snapshot is not reconstructed here.
Do not install their expected-failure markings on a newer branch.

Use `../sandbox-held-out-928755f-20260720/` for the normal-assertion rerun pinned
to a reproducible sandbox commit. See `../../docs/held-out-clearing-report.md`
for the original results and their limitations.
