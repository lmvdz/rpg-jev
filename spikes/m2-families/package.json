{
  "name": "@rpg-jev/spike-m2-families",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dry": "node src/run.ts --dry",
    "probe": "node --env-file-if-exists=../../.env src/run.ts"
  },
  "dependencies": {
    "@rpg-jev/core": "workspace:*",
    "@rpg-jev/jev": "workspace:*"
  }
}
